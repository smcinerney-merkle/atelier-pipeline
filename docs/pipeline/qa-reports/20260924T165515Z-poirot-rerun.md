## DoR: Diff Metadata
**Base:** 831d55a (working tree, uncommitted)
**Files in scope:** 6 path guards (`source/claude/hooks/enforce-{colby,sarah,agatha,ux,product,ellis}-paths.sh`) + their `.claude/hooks/` mirrors, 6 agent contracts (`source/shared/agents/{colby,sarah,agatha,sable-ux,robert-spec,ellis}.md`) + `.claude/agents/` mirrors, `source/shared/hooks/hook-lib.sh` (+ mirrors), 6 frontmatter overlays, `.claude/settings.json`, new `tests/hooks/test_friction_hook_port.py` (701 lines).
**Added:** ~436 lines across the 6 guards (per `git diff --stat`) + ~35 lines across the 6 agent contracts + 2 new `hook-lib.sh` functions (`hook_lib_agent_type_matches`, `hook_lib_agent_base_type`).
**Functions/behavior modified:** per-guard self-gate (agent_type match, with named-instance/teammate support and a loud fallback when `hook-lib.sh` fails to source), a new deny-by-default `pipeline_state_dir` report-allowlist block inserted ahead of each agent's own allowlist (Agatha's `docs/*`, Sarah's `docs/architecture/*`, etc.).
**New dependencies:** none.

## Prior Findings — Status

| # | Original finding | Status | Evidence |
|---|---|---|---|
| 1 | FIX-REQUIRED: `pipeline_state_dir` exemption in the six guards exempted all of `docs/pipeline/*`, letting any of the six agents overwrite `pipeline-state.md`, `context-brief.md`, `error-patterns.md`, `investigation-ledger.md`. | **CLOSED** | Each guard now has a deny-by-default block: `docs/pipeline/*` is blocked (exit 2) unless the remainder is a direct child matching that agent's own report prefix (`last-build-*.md`/`last-fix-*.md`/`last-colby-*.md` for Colby, `last-adr-*.md` for Sarah, `last-agatha-*.md` for Agatha, `last-ux-*.md` for Sable-ux, `last-spec-*.md` for robert-spec, `last-commit-*.md`/`last-push*.md` for Ellis). Exercised directly against the real `.claude/hooks/*` scripts (bare and named-instance agent_type) for all four Eva-owned files plus `last-qa-report.md`/`last-case-file.md`: every guard returns exit 2/BLOCKED for every agent. Own-report writes return exit 0. The `*/*` arm (guards against `last-build-x/pipeline-state.md`-style nesting) also verified directly. Full pytest suite (`tests/hooks/test_friction_hook_port.py`, 308 passed / 18 skipped) covers this exhaustively including sibling-report cross-blocking and configured (non-default) state-dir paths. |
| 2 | NIT: Agatha's exemption interacted with her `docs/*` allowlist (the state-dir block needs to sit ahead of the broad allowlist, or `docs/*` reopens the hole). | **CLOSED** | In the current `enforce-agatha-paths.sh`, the `pipeline_state_dir` deny-by-default block runs immediately after the traversal check and *before* the `docs/*) exit 0` allowlist line -- the code comment now explicitly calls this out as "LOAD-BEARING, NOT REDUNDANT." Exercised: `docs/pipeline/pipeline-state.md` -> BLOCKED; `docs/pipeline/last-agatha-foo.md` -> exit 0; `docs/other-file.md` (outside state dir) -> exit 0, confirming the general `docs/*` allowlist still works for non-state-dir docs. |

## Exercised
Ran the full new test suite against the real hooks: `uvx --with pyyaml pytest tests/hooks/test_friction_hook_port.py` -> 308 passed, 18 skipped (skips are `pytest.mark.skip` cases where an agent's own allowlist already covers `docs/pipeline`, by design).
Independently, piped hand-built PreToolUse JSON payloads into the real `.claude/hooks/enforce-{colby,sarah,agatha,ellis}-paths.sh` scripts (`CLAUDE_PROJECT_DIR=<repo>`, `ATELIER_SETUP_MODE` unset) covering:
- Trailing-slash and double-trailing-slash `pipeline_state_dir` config values (scratch copy in `/tmp`, deleted after) -- correctly stripped, behavior unchanged.
- Absolute file paths both inside and outside the project root -- normalized/rejected before the state-dir check runs (pre-existing logic, unaffected by this diff).
- Case-sensitivity of the report-prefix glob (`LAST-BUILD-x.md` -> BLOCKED, over-restrictive but not a hole).
- `docs/pipeline//last-build-x.md` and `docs/pipeline/./last-build-x.md` -> both BLOCKED (over-restrictive, not a hole).
- Named-instance agent_type spawns (`colby-w3-fix`, `ellis-cardfix`) for both the state-dir allowlist and the state-dir deny.
- Bystander agent types (`poirot-segment`, `investigator`) against Colby's guard on a path that would be BLOCKED for Colby -- correctly pass through (exit 0), confirming the self-gate still isn't enforcing another agent's guard.
- Confirmed byte-for-byte parity: `source/claude/agents/{agent}.frontmatter.yml` + `source/shared/agents/{agent}.md` assembled == `.claude/agents/{agent}.md` for all six agents.
- Confirmed `.claude/settings.json` is valid JSON and registers all six guards under `Write|Edit|MultiEdit` (Sarah under `Write|Edit`), matching the per-agent frontmatter registrations.
`git status --short` line count unchanged (49) after all exercising -- no mutation of the working tree; all scratch work was done in `/tmp` and deleted.

## DoD: Verification
**Findings:** 0 new (both prior findings CLOSED) | **Categories checked:** logic (glob ordering, deny-before-allow), boundary handling (trailing slash, absolute path, traversal), naming (report-prefix contract-vs-guard parity), cross-layer wiring (frontmatter <-> settings.json <-> installed `.claude/` mirrors) | **Grep verified:** report-prefix arms in each guard vs. each agent's `<output>` contract; `hooks:` record-form registration in all six frontmatter overlays | **Exercised:** full pytest suite (308 passed/18 skipped) + hand-built payloads against real `.claude/hooks/enforce-{colby,sarah,agatha,ellis}-paths.sh` for trailing-slash config, absolute paths, case-sensitivity, double-slash/dot-segment paths, named-instance agent_type, and bystander pass-through.

## Findings
| # | Location | Severity | Category | Description | Suggested Fix |
|---|----------|----------|----------|-------------|---------------|
