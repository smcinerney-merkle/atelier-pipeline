## DoR: Diff Metadata (scoped re-run)
**Files:** 25 tracked M + 1 untracked test file (widened from the prior run: fix touched `source/shared/agents/{agatha,colby,ellis,robert-spec,sable-ux,sarah}.md` and their `.claude/agents/` mirrors, which were untouched in the prior diff) | **Added:** ~10 | **Removed:** ~12 (net removal -- the fix deletes text, adds none)
**Functions modified:** none (Markdown persona docs only) | **New dependencies:** none

Re-produced via `git diff 8b22771 -- . ':(exclude)docs/pipeline'` plus the
untracked `tests/test_cursor_agent_parity.py`. `docs/pipeline/` excluded per
instructions (including the two untracked files there). CHANGELOG NIT from
the prior report is intentionally not re-checked -- left to Ellis per
instruction.

**Prior finding under re-review:** ".cursor-plugin/agents/{colby,sarah,agatha,ellis,robert-spec,sable-ux}.md `<output>` sections assert a per-file path guard ('the path guard blocks every other file there, including Eva's') that `.cursor-plugin/hooks/enforce-paths.sh` does not implement."

**The fix applied:** the exact clause "; the path guard blocks every other
file there, including Eva's" was deleted from all six personas' `<output>`
sections, in all three trees (`source/shared/agents/`, `.claude/agents/`,
`.cursor-plugin/agents/`) -- consistent with the single-source-of-truth
architecture (`.cursor-plugin` and `.claude` are both generated from the
same shared body, so the sentence could not be removed from Cursor only).
No change to `.cursor-plugin/hooks/enforce-paths.sh` or
`enforcement-config.json` (confirmed: `git diff 8b22771 --
.cursor-plugin/hooks/` is empty).

## Exercised
- Ran the full pytest suite: 803 passed, 18 skipped (up from 789 in the
  prior run -- the six now-touched `.claude/agents/` files add new
  parametrized cases to existing tests; no new failures).
- Diffed each `.claude/agents/{agatha,colby,ellis,robert-spec,sable-ux,sarah}.md`
  body (post-frontmatter) against its `source/shared/agents/` counterpart:
  byte-for-byte match, all six.
- Ran `tests/test_cursor_agent_parity.py` filtered to the six touched
  agents: 8 passed (parametrized checks for missing/extra + body match).
- Re-invoked `.cursor-plugin/hooks/enforce-paths.sh` directly with the same
  three probes from the prior report, post-fix:
  - `agent_type=sarah`, `docs/pipeline/last-adr-test.md` -> still
    `BLOCKED: Sarah can only write to docs/architecture/.` (exit 2).
  - `agent_type=colby`, `docs/pipeline/last-build-test.md` -> still
    `BLOCKED: Colby cannot write to blocked path.` (exit 2).
  - `agent_type=agatha`, `docs/pipeline/pipeline-state.md` (Eva's file) ->
    still exit 0, not blocked.
  Confirms the underlying mechanical behavior is byte-for-byte unchanged --
  only the prose claim about it moved.
- Confirmed `git status --short` unchanged before/after all exercises
  (only the untracked report file this run adds).

## DoD: Verification
**Findings:** 1 (1 FIX-REQUIRED, re-scoped; 0 new) | **Categories checked:** logic/wiring (re-verified via execution), regression sweep of the fix's own diff (naming, dead code, omission) | **Grep verified:** clause removed identically in all three trees for all six agents; no other Cursor persona (`robert`, `sable`, `sentinel`, `sherlock`, `distillator`, `investigator`) carries the same clause, so no widening of scope was missed | **Exercised:** full pytest suite (803 passed / 18 skipped), `test_cursor_agent_parity.py` subset, `.cursor-plugin/hooks/enforce-paths.sh` re-invoked for the same 3 probes as the prior run, byte-diff of `.claude` vs `source/shared` bodies for all 6 agents

## Verdict on the prior finding
**OPEN -- partially addressed.** The literal false sentence ("the path guard
blocks every other file there, including Eva's") is gone from all six
personas in all three trees; that specific overclaim no longer exists to
mislead a reader about Cursor's mechanical enforcement. But the finding's
substance -- verified by direct execution, both before and after this fix
-- is the same: under Cursor, Colby and Sarah are still mechanically
blocked from writing *any* file under `docs/pipeline/`, including the exact
report path their own `<output>` sections instruct them to write to
(`last-build-<slug>.md` / `last-fix-<slug>.md` / `last-colby-<slug>.md` for
Colby, `last-adr-<slug>.md` for Sarah), and Agatha is still *not* restricted
to her own report filename -- she can freely write `docs/pipeline/pipeline-state.md`,
contradicting her own remaining sentence ("These are the only files you may
write under `docs/pipeline`."). The fix removed the inaccurate narration
about the guard; it did not touch `.cursor-plugin/hooks/enforce-paths.sh`
or `enforcement-config.json`, so the reachability break and the
over-permissive Agatha case are unchanged. Re-closing this fully requires
either implementing a per-report allowlist in the Cursor hook (matching
what the `.claude` per-agent hooks already do, verified via
`test_contract_matches_guard`) or routing Colby/Sarah/Agatha report writes
in Cursor through a path that hook actually permits (e.g. `docs/architecture/`
for Sarah, main-thread/Eva-owned write for Colby and Sarah since neither
`colby` nor `sarah` agent_type has any `docs/pipeline` allowance at all).

## Findings
| # | Location | Severity | Category | Description | Suggested Fix |
|---|----------|----------|----------|-------------|---------------|
| 1 | `.cursor-plugin/agents/{colby,sarah}.md` `<output>`, and `.cursor-plugin/agents/agatha.md` `<output>` | FIX-REQUIRED (re-opened, narrowed) | logic/cross-layer wiring | Re-verified by direct execution after the fix: `.cursor-plugin/hooks/enforce-paths.sh` (unchanged by this fix) still blocks Colby and Sarah from writing *any* file under `docs/pipeline/`, so their own report-path instructions remain unreachable in Cursor; Agatha remains unrestricted to her own report filename and can still write Eva's `pipeline-state.md`, contradicting her surviving "only files you may write" sentence. The fix corrected the false narration about the guard but did not touch the guard itself, so the mechanical mismatch the finding was built on persists. | Implement a per-report allowlist in `.cursor-plugin/hooks/enforce-paths.sh` for `sarah`/`colby`/`agatha` (mirroring the Claude per-agent hooks already verified by `test_contract_matches_guard`), or change how Colby/Sarah reports get written under Cursor (e.g. main-thread/Eva write on their behalf) so the instruction matches what the hook permits. Extend a parity/contract test to cover `.cursor-plugin/agents` against `.cursor-plugin/hooks/enforce-paths.sh` the way `test_contract_matches_guard` covers the Claude side, so this class of drift is caught mechanically going forward. |
