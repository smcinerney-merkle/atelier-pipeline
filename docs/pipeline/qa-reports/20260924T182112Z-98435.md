## DoR: Diff Metadata
**Files:** 20 changed (18 tracked M + 1 untracked test file + test_friction_hook_port.py extension) | **Added:** ~180 | **Removed:** ~35
**Functions modified:** none (all Markdown persona docs, JSON version fields, and two pytest test files -- no application code) | **New dependencies:** none

Scope note: docs/pipeline/ was excluded per instructions except two untracked
files under it (`last-build-cursor-resync.md`, `last-commit-friction-port.md`)
that were also out of scope by rule ("Files under docs/pipeline/ are out of
scope"). Not reviewed.

Diff shape: version bump 5.2.0->5.2.1 in both plugin manifests; rewritten
`<workflow>`/`<output>` sections across most `.cursor-plugin/agents/*.md`
files (mostly catching Cursor copies up to content already present in
`source/shared/agents/*.md` at HEAD, plus new report-path instructions
telling each agent where to write its own status report under
`docs/pipeline/`); one behavioral change to `.claude/agents/colby.md` /
`source/shared/agents/colby.md` (report path for contracts tables moved off
`pipeline-state.md`); a new structural test file
`tests/test_cursor_agent_parity.py` (byte-for-byte parity check between
`source/shared/agents/` and `.cursor-plugin/agents/`); and an extension to
`tests/hooks/test_friction_hook_port.py` covering the Colby report-path
change.

## Exercised
- Ran the new `tests/test_cursor_agent_parity.py` and the extended
  `tests/hooks/test_friction_hook_port.py`: 328 passed, 18 skipped.
- Ran the full suite (`tests/`): 789 passed, 18 skipped -- no regressions.
- Invoked the actual mechanical path-guard hook the new Cursor persona text
  makes claims about (`.cursor-plugin/hooks/enforce-paths.sh`) directly,
  feeding it the exact report paths the diff newly tells each agent to
  write to:
  - `agent_type=sarah`, `file_path=docs/pipeline/last-adr-test.md` ->
    `BLOCKED: Sarah can only write to docs/architecture/.` (exit 2) --
    Sarah's own new `<output>` instruction ("Write your report ... to
    `docs/pipeline/last-adr-<slug>.md`") is unreachable under Cursor.
  - `agent_type=colby`, `file_path=docs/pipeline/last-build-test.md` ->
    `BLOCKED: Colby cannot write to blocked path.` (exit 2) -- Colby's own
    new `<output>` instruction (`last-build-<slug>.md` /
    `last-fix-<slug>.md` / `last-colby-<slug>.md`) is unreachable; the
    hook's `colby_blocked_paths` config blocks the entire `docs/` prefix.
  - `agent_type=agatha`, `file_path=docs/pipeline/pipeline-state.md` (Eva's
    file) -> exit 0, **not blocked** -- contradicts the new claim added to
    `.cursor-plugin/agents/agatha.md`: "These are the only files you may
    write under `docs/pipeline`; the path guard blocks every other file
    there, including Eva's."
  - `agent_type=robert-spec`, `file_path=docs/product/foo-spec.md` ->
    `BLOCKED: Agent 'robert-spec' does not have write access.` (exit 2) --
    pre-existing catch-all behavior, not introduced by this diff, but the
    diff adds new robert-spec instructions assuming Write access exists.
- Confirmed `git status --short` unchanged after all exercises (read-only on
  the repo; no scratch copy was needed since the hook was invoked directly
  with no side effects on this tree).

## DoD: Verification
**Findings:** 2 (1 FIX-REQUIRED, 1 NIT) | **Categories checked:** logic/wiring (cross-layer claim vs. mechanical enforcement), naming, dead code, resources, concurrency, type safety, security, error handling -- swept, nothing else surfaced | **Grep verified:** `lint_command`/`typecheck_command` placeholder consistency across all three Colby copies; `REPORT_PREFIXES`/`test_contract_matches_guard` parametrization confirmed it only covers `source/shared/agents` + `.claude/agents`, not `.cursor-plugin/agents` | **Exercised:** full pytest suite (789 passed / 18 skipped), new parity test file, `.cursor-plugin/hooks/enforce-paths.sh` invoked directly for 4 representative agent/path combinations

## Findings
| # | Location | Severity | Category | Description | Suggested Fix |
|---|----------|----------|----------|-------------|---------------|
| 1 | `.cursor-plugin/agents/{colby,sarah,agatha,ellis,robert-spec,sable-ux}.md` `<output>` sections (new text in this diff) | FIX-REQUIRED | logic/cross-layer wiring | New persona text asserts a per-file "path guard blocks every other file there, including Eva's" for each agent's `docs/pipeline/last-*.md` report path. Verified by direct invocation that the actual Cursor mechanical guard (`.cursor-plugin/hooks/enforce-paths.sh`) is coarser and, for several agents, contradicts the claim outright: Sarah and Colby are blocked from writing *any* file under `docs/pipeline/` (including the exact report path the new text tells them to write), while Agatha is *not* restricted to her own filename and can freely write `docs/pipeline/pipeline-state.md` (Eva's file). Only the Claude-side per-agent hooks (`enforce-*-paths.sh`) implement the fine-grained per-report allowlist this text describes; the Cursor consolidated hook does not, and this diff's new test (`test_contract_matches_guard`) only checks `source/shared/agents` and `.claude/agents`, not `.cursor-plugin/agents`, so nothing catches the mismatch. | Either (a) update `.cursor-plugin/hooks/enforce-paths.sh` to implement the same per-report allowlist Claude's hooks use for `sarah`/`colby`/`agatha`/etc. before shipping this persona text, or (b) soften the new Cursor-copy `<output>` wording to not claim a guarantee the Cursor guard doesn't provide, and extend `test_contract_matches_guard` (or an equivalent) to cover `.cursor-plugin/agents` against `.cursor-plugin/hooks/enforce-paths.sh`. |
| 2 | `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `.cursor-plugin/plugin.json`, `.cursor-plugin/marketplace.json` | NIT | omission | Version bumped 5.2.0 -> 5.2.1 in all four manifest files consistently, but `CHANGELOG.md` has no 5.2.1 entry yet (last entry is 5.2.0, 2026-06-17). | Add the CHANGELOG entry (likely Ellis's job at commit time per project convention) before/at release. |
