## DoR: Diff Metadata
**Files:** 24 sections (14 modified, 4 new untracked test/hook files, 1 new hook file installed to two locations, plus manifest/changelog/rules mirrors) | **Added:** ~950 | **Removed:** ~90
**Functions modified:** `enforce-git.sh` (TOOL_NAME/COMMAND jq guards, Ellis match via `hook_lib_agent_type_matches`, test-runner regex, new `_agent_matches` fallback), `prompt-compact-advisory.sh` (Ellis match), `hook-lib.sh` (new `hook_lib_agent_personas`), new `enforce-spawn-name.sh` (PreToolUse Agent gate, G-142) | **New dependencies:** none

## Exercised
Ran `enforce-spawn-name.sh` directly with 15+ hand-built JSON payloads via `bash -c 'echo ... | hook'`, unsetting `ATELIER_SETUP_MODE`:
- Unnamed spawn, exact-match name, `<type>-<suffix>` name → allow (exit 0), matches doc.
- `subagent_type: claude, name: ellis-gd-tag` and `subagent_type: colby, name: agatha-disguised` (the two real incidents cited in the header) → both BLOCKED (exit 2), matches doc.
- OVERLAP case `subagent_type: sable, name: sable-ux-1` → BLOCKED with `sable-ux` cited as the resolved base; the inverse (`subagent_type: sable-ux, name: sable-ux-1`) → allowed. Matches doc exactly.
- `investigator-segment` (correct Poirot convention) → allow; `poirot-segment` under `subagent_type: investigator` (Friction's old convention) → now correctly BLOCKED, matching the changelog's stated behavior change.
- Named spawn with no `subagent_type` → blocked with the documented message.
- Fail-closed check: copied the hook to a directory with no `hook-lib.sh` → BLOCKED (exit 2), confirming the "fails closed" claim (this hook deliberately differs from the repo's usual fail-open convention).
- Non-Agent tool_name and malformed JSON → both allow (exit 0), no crash.
- Realistic naming patterns already used elsewhere in the pipeline (`scout-1`, `colby-teammate-1`, `sarah-adr-revise`, `robert-spec-1`, `robert-1`) → all allowed, no false-positive blocks.

Ran `enforce-git.sh` directly with 11 payloads covering: `npm run test:e2e` / `npx vitest run` / `npx playwright test` leaks (now correctly BLOCKED for a non-allowlisted agent, per G-143's stated fix), `npx playwright install` (correctly NOT treated as test execution), main-thread (empty `agent_type`) running pytest (now allowed, matches new doc), sherlock blocked, `investigator` and a named `investigator-seg1` instance allowed, named Ellis instance (`ellis-gd-tag`) permitted to commit, Eva (empty agent_type) blocked from committing. Also verified the two RF-inversion cases named in the changelog: `git switch --discard-changes` still leaks (documented open gap, confirmed unchanged) and `git checkout HEAD~1 -- file.txt` (tree-ish pathspec) is now correctly BLOCKED.

Ran the full test suite: `tests/hooks/test_enforce_git_rf_parity.py`, `test_if_conditionals.py`, `test_enforce_spawn_name.py`, `test_prompt_compact_advisory.py` (240 passed), then the entire `tests/` suite (1033 passed, 18 skipped, 0 failed).

Diffed installed copies against source templates (`enforce-git.sh`, `hook-lib.sh` x2, `prompt-compact-advisory.sh`, `enforce-spawn-name.sh`) — all byte-identical, satisfying the project's triple-target sync convention.

Confirmed via `settings.json` inspection that `enforce-spawn-name.sh` is registered on the `Agent` matcher with no `if`, positioned second (right after `enforce-brain-capture-gate.sh`), matching the changelog and hooks.md claims.

Confirmed `enforce-brain-capture-pending.sh`'s separate allowlist was not repointed at the new `hook_lib_agent_personas` function (the diff's own header warns against this) — grep shows zero call sites outside `enforce-spawn-name.sh`.

Confirmed Cursor has no `enforce-spawn-name.sh` registration in `.cursor-plugin/hooks/hooks.json` (intentional per diff comments — Cursor's hook schema has no Agent-tool event).

## DoD: Verification
**Findings:** 0 (confident) | **Categories checked:** logic/boundary (regex anchoring, prefix-matching overlap resolution), error handling (fail-open vs. deliberate fail-closed, `set -e` interaction with jq failures), naming (persona roster single-source-of-truth), dead code (none found), cross-layer wiring (settings.json registration order, source/installed parity, Cursor non-parity), concurrency (n/a — single-shot hooks), type safety (n/a — bash). **Grep verified:** hook registration positions, installed-vs-source diffs, personas-function call sites, Cursor hooks.json absence. **Exercised:** `enforce-spawn-name.sh` (15+ payloads incl. fail-closed), `enforce-git.sh` (11 payloads), full pytest suite (1033 passed).

## Findings
| # | Location | Severity | Category | Description | Suggested Fix |
|---|----------|----------|----------|-------------|---------------|
